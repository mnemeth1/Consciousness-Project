# P3M04 — methodology page ("How this project works"), site 0.4.9 / thesis-v1.0.4

Status: published. Authority: project owner approval of the plan and instruction to create the page, 15 September 2026, under the blanket stage authorization of 2026-09-14; owner publication instruction of the same day, with the recorded decision that no distinct-model delta review is required because no claim-bearing text of the thesis, article or overview changed (`state/acceptance_P3G2.json` amendment P3G2-A4).

## What this milestone adds

One new public page, `paper/methodology.html`, presenting the project's method as a named deliverable alongside the overview, article and thesis. The page describes the recorded procedure only: the nine-component decomposition and the report/event distinction, the withheld gates and removal-scenario sensitivity tests, the independent-review rules, the crosswalk and ledger tracing, the hash-bound immutable releases, and the AI-assistance disclosure with its limits. It states no verdict and introduces no evidence; every factual statement about the procedure names, or is checkable against, a public record.

## Changes

- **New page**: `paper/methodology.html` (eight sections, sticky contents rail, the shared site shell; `#version-notice`, meta and visible version 0.4.9 / 2026-09-15).
- **Pipeline registration** (renderer recipe changes, so `renderer_sha256` changes with this version):
  - `scripts/paper/common.cjs`: `methodology_html_sha256` in `AUX_SOURCES`; `methodology.html` in `ARTIFACT_FILES`; the field in the `assertSameRelease` immutability list.
  - `scripts/paper/build.cjs`: the page is read, inspected (`inspectAuxiliary`), hashed into the build manifest, written into the artifact and validated; the landing page must now link it; `validatePair` pins a seven-file artifact.
  - `scripts/paper/release.cjs` artifact names; `scripts/validate_paper_artifact.py` aux fields, expected set and hash pairs; `scripts/validate_public_snapshot.cjs` release-state prose checks now include the page.
  - `tests/paper.test.cjs`: methodology fixture; the bound-artifact test now expects seven files and seven published assets and binds the new hash field.
  - `scripts/thesis/build.cjs`: the navigation template gains the Method link (template, not part of the pinned renderer recipe).
- **Navigation and landing**: every page's `.navlinks` row gains "Method"; the landing's reading-path grid becomes "Four ways in" with a methodology card. No other prose on the existing pages changed.
- **Version cascade**: article 0.4.9 (metadata and navigation only), thesis-v1.0.4 (presentation-only republication: version metadata and the navigation link; regenerated by `scripts/thesis/build.cjs --publish`). `paper/lay_crosswalk.json` paper_version, `thesis/thesis.json` and all seventeen `thesis/crosswalk/*.json` thesis_version updated.
- **Records**: `paper/CHANGELOG.md` 0.4.9; `state/acceptance_P3G2.json` amendment P3G2-A4 (review not required by owner decision); `paper/draft-release.json` 0.4.9 with the new `methodology_html_sha256` binding; `state/thesis_release.json` 1.0.4; `state/public_status.json`, `STATUS.md`, `CURRENT_RELEASE.md`, `README.md`; `PUBLICATION_MANIFEST.json` gains `paper/methodology.html` (explicit `--add`).

## Verification already run

`node --test tests/paper.test.cjs` (27/27 pass); fresh preview build of the seven-file artifact with `validatePair` (pass, including the methodology page's `inspectAuxiliary` contract); `node scripts/export_public_snapshot.cjs` (validator passes inside the export); `node scripts/thesis/release.cjs render` (1.0.4 renders). An adversarial fact-check pass over the page's factual statements against their named records preceded publication; it found one dropped limitation (the standing-limitations list omitted "version identity") and five overstatements, and all corrections are applied with the hashes bound afterward.

## Review disposition

Nothing in this milestone changes a verdict, hedge, withheld gate, gap disposition, citation or any claim-bearing paragraph of the article, overview or thesis. On that basis the owner recorded that no distinct-model delta review is required for this milestone (P3G2-A4); the adversarial fact-check above stands as the pre-publication check of the page, and the prepared method-1 review prompt is retained in `work/P3M04/` as an optional future check. This is not external peer review and establishes no metaphysical result. All twelve withheld stronger-conclusion gates remain withheld.
