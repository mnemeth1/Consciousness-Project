# P3M01 — post-release maintenance decisions (thesis 1.0.1 / site 0.4.6)

Date: 2026-09-15 (evening of 14 September, Pacific). Executor: Claude Fable 5 session (Cowork), under the project owner's blanket stage authorization of 2026-09-14 and the owner's instruction of 2026-09-15, "use your best judgement to execute the plan" (the plan being the verification of the published-page review in the project record). Decisions the plan left to the owner were taken here as recommended in that plan; each is reversible before the push.

- **D9 — Patch release, not a footnote.** Thesis 1.0.1 and site 0.4.6, rather than a front-matter note that in-text status statements are dated. The Declarations, chapter 11's Limitations and chapter 12 asserted facts about the review record that were false at release; a released thesis whose credibility rests on record fidelity should not carry them under a disclaimer when the edit is ten paragraphs. `docs/PAPER_RELEASE.md`: patch versions for corrections or presentation changes; changed bytes require a new documented version.
- **D10 — Renderer changes in `scripts/thesis/build.cjs`.** Multi-source `[@S-a; @S-b]` and record-reference `[@A-…]`/`[@C-…]`/`[@CL-…]` citations render; any unrendered `[@` fails the build; every inline citation must appear in the paragraph's crosswalk entry; `bibliography_note` renders; released-stage status language is linted with an allowlist in `thesis/thesis.json`. This file is outside the pinned renderer set (`scripts/paper/*`, lockfile, workflow), so the approved renderer recipe is unchanged (`e68a373a…`). The single-source citation form renders byte-identically to earlier builds.
- **D11 — S-T10-002 keeps one identity with two inspected versions.** The P2T04 packet already decided this (`work/P2T04/source_identity_annex_03.json`: reuse the canonical Monadology identity with the 1998 translation as version V-P2T04-004); the ledger record simply never received the `versions` entry other records carry. The entry is copied verbatim from `work/P2T04/source_versions.json`; locators and a bibliography note are added; no claim, locator, acceptance or source identity changes. A new source id would have forced chapter-3 citation and crosswalk edits for no gain in honesty.
- **D12 — Provenance notes, not new hosts.** S-T14-003 (Fischer, third-party rehost) and S-T19-005 (Woerlee, web-hosted PDF) gain `bibliography_note` fields naming the original publications and stating that they were not inspected at that pagination. No new URL is introduced, because nothing was inspected at a new host and the project does not cite what it did not inspect.
- **D13 — Hashed submission artifacts are not edited.** The `draft_pending_R18`/`submitted` labels inside `work/T17/*.json` and `work/T16/*.json` are superseded by a pointer record, `state/T16_T18_status_note.json`, per the historical-input policy in `state/submission_T17.json`.
- **D14 — Chapter statuses stay `accepted`; the release record is amended.** Following the P3R20 cycle-1 precedent (three bounded corrections, delta re-review, no status flip, acceptance-time hashes left as the record of what was accepted), the corrections are recorded in `work/P3M01/changes.json` and in amendment P3G2-A1 of `state/acceptance_P3G2.json`, which the thesis release authorization binds. The `lennox-voice` skill's procedure for a *rewritten* chapter (flip to `submitted`, full re-review) is for rewrites, not for ten status sentences.
- **D15 — Nothing publishes before the delta review accepts.** `state/thesis_release.json` carries a placeholder P3G2 hash until `work/P3M01/finalize.cjs` records an accepting `delta-1` run; a premature push fails the thesis release job rather than publishing a half-reviewed patch.
- **D16 — The 53-minor sweep stays a separate cycle.** Only the five leftovers the delta report had already named (c03-p035, c01-p026 crosswalk, c02-p038 crosswalk, apB-p010 crosswalk, apC-p011) ride along, so the delta review stays bounded.
- **D17 — c12-p025's promise of "exact deltas from the article and the frozen version 1" is not restated as done.** No delta artifact is named in the P3G2 record. Flagged to the owner in `report.md` as a possibly unmet plan deliverable rather than asserted.
- **D18 — The article's Enoch 2014 citation is left alone.** The record shows the journal PDF was inspected with journal pagination, so no manuscript-pagination note is owed; changing article text for a non-defect would widen the 0.4.6 diff beyond metadata and a navigation label.
