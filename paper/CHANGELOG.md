# Paper versions

## 0.4.12 - 2026-09-15

Navigation and PDF-delivery consistency pass (owner instruction of 2026-09-15; presentation-only, no argument text changed). Every page's top navigation now carries the same two boxed buttons, "Article PDF" and "Thesis PDF": the thesis page gains the previously missing Article PDF button (emitted by `scripts/thesis/build.cjs`), and the Thesis PDF button is promoted from the borderless ghost style to the same outlined primary style as the Article PDF button. The thesis PDF is now also served from the site itself: the thesis render stages its verified `thesis.pdf` with its provenance manifest `thesis-release.json` into the deployed set (grown from eleven to thirteen files across `validatePair`, `ARTIFACT_FILES`, the release uploader, the standalone artifact validator and the CI step order), so the button links the relative `thesis.pdf` and the document opens in the browser exactly as `paper.pdf` does, instead of forcing a download from the immutable release asset; the release asset remains published unchanged under the `thesis-v` tag. The article's print stylesheet is repaired so the generated `paper.pdf` matches the thesis PDF's layout: the screen navigation bar no longer prints (its inline `display` previously defeated the un-`!important` print rule), the text column fills the printable width instead of sitting left at its 66ch screen measure, and heading sizes print at the thesis PDF's point scale. The article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld.

## 0.4.11 - 2026-09-15

Completes the discovery-metadata pass (owner instruction of 2026-09-15; presentation-only, no argument text changed). Every page gains a `link rel=canonical` to its own URL, a favicon (`favicon.png`, 64px, from the owner-supplied artwork) and Open Graph image tags pointing at a new 1200×630 social preview image (`social-preview.jpg`, likewise owner-supplied), with the Twitter card upgraded to `summary_large_image`; meta descriptions are tightened to roughly 130–165 characters with the calibration language preserved. The landing page carries WebSite JSON-LD, the article ScholarlyArticle JSON-LD and the thesis Thesis JSON-LD, each restating the page's title, contributors, version, canonical URL and not-externally-peer-reviewed status. Both image assets join the hash-bound artifact (`favicon_sha256`, `social_image_sha256` in the release records), growing the pinned deployed set from nine to eleven files. The page security contract is relaxed exactly three ways, enforced by the build inspectors: canonical links with https targets, icon links to artifact files, and inert `application/ld+json` blocks that must parse as JSON; scripts, external resources and every other restricted element remain banned. The article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld.

## 0.4.10 - 2026-09-15

Discovery and search-engine metadata only (owner instruction of 2026-09-15; presentation-only, no argument text changed). Every site page gains a meta description, Open Graph tags (site name, type, canonical URL, title, description) and a Twitter summary-card tag; the thesis page's tags are emitted by `scripts/thesis/build.cjs`. The build now also generates and validates two deterministic discovery files in the published artifact, `robots.txt` (allow all, sitemap pointer) and `sitemap.xml` (the five pages with the release date as lastmod), growing the pinned artifact listing from seven to nine files, enforced byte-for-byte against the generator by `validatePair` and mirrored in the release uploader and the tests. The security contract is unchanged: no scripts, no link elements, no external resources; canonical signaling uses og:url and the sitemap. The article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld.

## 0.4.9 - 2026-09-15

Adds a fifth site page, `methodology.html` ("How this project works"), presenting the project's method as a named deliverable alongside the overview, article and thesis (owner approval of 2026-09-15 of the P3M04 plan). The page describes the recorded procedure only: the nine-component decomposition and the report/event distinction, the withheld gates and removal-scenario sensitivity tests, the independent-review rules, the crosswalk and ledger tracing, the hash-bound immutable releases and the AI-assistance disclosure, with every factual statement linked to a public record; it states no verdict and introduces no evidence. The page joins the bound release set: the build now inspects, hashes, publishes and validates seven artifact files, the release records gain a `methodology_html_sha256` binding, and the top navigation of every page and the start-page reading paths gain a "Method" entry. The article argument, overview and thesis text are unchanged (version metadata and navigation only); the thesis republishes as thesis-v1.0.4 solely because published page hashes are immutable per version. The article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld.

## 0.4.8 - 2026-09-15

Adds a thesis PDF download to the site menu (owner request of 2026-09-15). Every page's top navigation now offers both documents: the existing article PDF button is relabeled "Article PDF" and a new "Thesis PDF" button links the immutable release asset at `releases/download/thesis-v1.0.3/thesis.pdf`; the thesis page, which previously had no download button, gains the same "Thesis PDF" button, generated by `scripts/thesis/build.cjs`. This is presentation-only and republishes the thesis as thesis-v1.0.3 solely because release assets and the thesis page hash are immutable per version (`state/acceptance_P3G2.json` amendment P3G2-A3): no thesis paragraph, citation or References entry changed, and the article argument is unchanged (version metadata only). The article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld.

## 0.4.7 - 2026-09-15

Ships thesis-v1.0.2 on the site. A review of the released thesis 1.0.1 found nine record-fidelity items and a set of References rendering defects, none touching a verdict. Thesis 1.0.2 corrects twenty paragraphs (exact before/after in `work/P3M02/changes.json`): a wrong publisher in a bibliography note and a wrong crosswalk pointer, both introduced in 1.0.1; a misgendered witness, a misdescribed typology, two wrong chapter cross-references, a mislocated synthesis-audit stage and four loose labels, all pre-existing; and four chapter statements of standing limitations that Appendix C attributed to paragraphs which did not carry them. Eight Appendix C rows are re-pointed to the paragraphs that actually state their limitation (amendment P3G1-A1 in `state/acceptance_P3G1.json`; no disposition changes). The Sober 2009 source record now carries the P2T11 complete-scan inspection (`records/decisions.json` P3-LEDGER-002). The References now link DOIs through doi.org, omit an empty author segment and no longer double punctuation; the build lints record paths named in prose against the crosswalk, and the snapshot validator binds the lay crosswalk version. Both changes were re-reviewed by the distinct-model reviewer before publication (`state/acceptance_P3G2.json` amendment P3G2-A2). No verdict, hedge, withheld gate, gap disposition or citation changed. The plain-language companion is rewritten in the thesis's voice with six crosswalk entries extended; its calibration follows the article (before/after in `work/P3M02/companion_changes.json`). The article argument is unchanged (version metadata only); the article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld. The site is visually redesigned across all four pages (start page, article, overview, thesis): a shared warm-paper style with serif system fallbacks, a top navigation bar, a sticky contents rail, margin paragraph-id labels and hover citation previews, generated by `scripts/thesis/build.cjs` for the thesis page and applied as presentation-only markup to the others. No substantive sentence changed with the redesign: article and thesis paragraph text is identical to the reviewed content, and the overview keeps the rewritten companion text verbatim; the References now link an `http://` locator through `https://` so every in-article link satisfies the artifact contract.

## 0.4.6 - 2026-09-15

Ships the corrected thesis (thesis-v1.0.1) on the site. A review of the published pages after thesis-v1.0.0 found status statements left over from drafting (the Declarations and chapters 2, 3, 11 and 12, and Appendix B, still said the coverage gate, whole-thesis review and release adjudication had not been run), twelve citation brackets the renderer had passed through as text, and seven cited sources missing from the References as a result. Thesis 1.0.1 corrects ten paragraphs and twelve crosswalk labels (exact before/after in `work/P3M01/changes.json`), renders multi-source and record-reference citations (References 128 → 135), adds provenance notes to three bibliography entries, and lints released-stage status language at build time; the changes were re-reviewed by the distinct-model reviewer before publication (`state/acceptance_P3G2.json` amendment P3G2-A1). No verdict, hedge, withheld gate or citation changed. The site's start page, companion page and article navigation now describe the thesis as released rather than incomplete and no longer list P2R20/P2G2 as pending. The article argument is unchanged (version metadata and navigation label only); the article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld.

## 0.4.5 - 2026-09-15

Ships the released thesis (thesis-v1.0.0) on the site: all Phase 3 gates are closed with recorded decisions (P3G1 coverage, P3R20 whole-thesis adversarial review, P3G2 final adjudication), and P2R20/P2G2 close through them, so release records stop listing those reviews as pending. Three bounded P3R20 corrections are included (apD-p015 PSR clause per T17-06A, c11-p016 per T17-04, c06-p028 AWARE II citation to S-T07-007). The article argument is unchanged; the article remains an authorized working draft with scientific acceptance false, and all twelve withheld stronger-conclusion gates remain withheld.

## 0.4.4 - 2026-09-14

Republishes the thesis working draft in full: the complete abstract (the published page previously carried the pre-draft "abstract pending" placeholder frozen at 0.4.2), chapters 1–12 and appendices A–D, all internally accepted after the owner-adjudicated P3RX cross-model review (state/adjudication_P3RX.json). The thesis page banner now reports acceptance state accurately. The article argument is unchanged apart from version metadata. Coverage gate P3G1, whole-thesis review P3R20 and release adjudication P3G2 remain open; pending-review status (P2R20/P2G2) remains recorded in release records.

## 0.4.3 - 2026-09-14

Renderer determinism fix only; article, companion, landing and thesis content are unchanged apart from version metadata. The PDF renderer now strips the Chromium structure tree completely (outline /SE references removed and unreachable objects swept) so repeated renders of identical HTML produce identical PDF bytes. A new version is required because release assets are immutable and the renderer hash changed. Pending-review status (P2R20/P2G2) remains recorded in release records.

## 0.4.2 - 2026-09-14

Adds an incomplete thesis working draft (`thesis.html`) to the public site and a landing-page link to it. Chapters 1–10 of the thesis expansion are included as internally accepted text; front matter, chapters 11–12 and appendices A–D remain labeled skeletons. No thesis PDF is published. The article argument is unchanged apart from version/date metadata and a navigation link. Pending-review status (P2R20/P2G2) remains recorded in release records.

## 0.4.1 - 2026-09-13

Adds companion-style top navigation on the research article page: start page, plain-language overview, and PDF download. The article argument is unchanged. Pending-review status (P2R20/P2G2) remains recorded in release records.

## 0.4.0 - 2026-09-13

Adds a public landing page and a plain-language companion overview, published together with the article as one versioned artifact. The site root becomes a reader chooser; the research article moves to `paper.html` with an unchanged argument (version metadata only). The companion summarizes the article for general readers and is bound to it by a build-validated crosswalk (`paper/lay_crosswalk.json`) mapping every companion claim block to the article paragraphs it summarizes. The release pipeline, artifact validators and release records now bind the landing, companion and crosswalk hashes alongside the existing HTML/PDF pair.

Article content is unchanged from 0.3.0 apart from the version number. Pending-review status (P2R20/P2G2) and the absence of external peer review remain recorded and visible on all pages.

## 0.3.0 - 2026-09-13

Replaces the project-status summary with a complete philosophical article, **Consciousness, Existence, and the Grounds of Significance**. Develops a connected argument about experiential explanation, neural constraints, survival and timelessness, meaning and moral authority, and evil and hiddenness. Adds direct literature citations and a focused source crosswalk. HTML and PDF are generated and released together.

This is a versioned scholarly manuscript for criticism and further refinement. Publication does not assert external peer review or completion of the project's outstanding final source review and adjudication (P2R20/P2G2). Those workflow facts remain in release records rather than the article's argument.

## 0.2.0 - 2026-09-13

First public Phase 2 working draft. Summarizes the current comparative analysis and corrected source-eligibility sensitivity work. Adds matching generated HTML/PDF publication and an immutable GitHub prerelease. Final synthesis, independent final source review and release adjudication remain pending; all twelve stronger-conclusion gates remain withheld. This is not the completed or externally peer-reviewed Phase 2 paper.

Future releases must use a new version and describe changes. CI rebuilds the PDF from the exact HTML and deploys the pair together. See ../docs/PAPER_RELEASE.md for draft and reviewed release procedures.
